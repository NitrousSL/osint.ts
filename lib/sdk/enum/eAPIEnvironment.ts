/*
    * @return { APIEnvironment }
    * @description Used to define the environment of the API.
*/
export enum APIEnvironment {
    Development = 'development',
    Production  = 'production',
    Staging     = 'staging',
    Sandbox     = 'sandbox',
}

// Path: lib/sdk/enum/eAPIEnvironment.ts
