/*
    * @return { ModuleCategory }
    * @description Used to define the category of an OSINT module based on its query input.
 */
export enum ModuleCategory {
    Username = 'username',
    Phone    = 'phone',
    Email    = 'email',
    IP       = 'ip',
    Domain   = 'domain',
}

// Path: lib/sdk/enum/eModuleCategory.ts
